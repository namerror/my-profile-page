# Claude Code Entry

Use `AGENTS.md` as the authoritative guidance for this repo. Keep changes minimal, avoid large refactors without approval, and confirm before running heavy commands. Log each session in `docs/agent-logs/` per `docs/agent-logs/README.md`.
