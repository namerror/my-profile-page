# Codex Entry

Use `AGENTS.md` as the authoritative guidance for this repo. Prefer minimal diffs and avoid mutating commands unless requested. Log each session in `docs/agent-logs/` per `docs/agent-logs/README.md`.
