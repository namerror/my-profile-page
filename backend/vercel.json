{
  "buildCommand": "python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt && python -m alembic -c alembic.ini upgrade head",
  "outputDirectory": "backend",
  "env": {
    "PYTHONPATH": "backend"
  }
}