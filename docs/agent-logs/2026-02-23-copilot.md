# Session Summary
- Fixed overflow issue with skill tags in the Project and Learning admin dashboard manager tabs.
- Added `flex-wrap` to the outer status+skills wrapper div and the inner skills chip div in both the Project and Learning `renderItem` sections of `managerConfigs.tsx`, matching the existing pattern used by the Activities manager.

# Files Touched
- `frontend/app/admin/dashboard/managerConfigs.tsx`

# Tests / Verification
- Frontend lint passed (exit 0).
- Changes are CSS-only Tailwind class additions (`flex-wrap`) — no logic changes.

# Open Questions / Follow-ups
- None
